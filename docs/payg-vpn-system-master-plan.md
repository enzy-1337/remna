# Мастер-план: pay-as-you-go VPN + тарифы + Remnawave (переработка remna)

**Версия документа:** 1.0  
**Дата:** 2026-04-11  
**Статус:** живой документ — при изменении scope, решений или стека **обновляйте этот файл** и дату версии.

---

## 0. Зачем этот файл и как им пользоваться

### Для людей

- Это **единая точка входа**: требования, принятые решения, порядок работ, риски и ссылки на смежные документы.
- Перед началом эпика или спринта — **прочитать разделы 1–4**, затем актуальную **фазу** из раздела 6.
- После выполнения шага — отмечать чекбоксы в Git-вики / копии файла или в PR-описании (в самом репозитории чекбоксы в Markdown удобно тикать вручную при мерже).

### Для ИИ-ассистента (Cursor и т.п.)

**Обязательно:**

1. В начале сессии по задачам «биллинг / тарифы / VPN / Remnawave / бот-детализация» — **прочитать этот файл целиком** или минимум §1–6 и текущую фазу.
2. Не начинать крупные архитектурные отклонения (смена СУБД, отказ от вебхуков панели, другая модель денег) **без явного указания пользователя** или обновления этого документа.
3. Если требование **противоречит** коду или документу — **остановиться** и задать пользователю уточняющий вопрос, предложив правку этого файла.
4. После согласования нового решения — **внести правку в этот файл** (и дать краткое резюме в ответе).

### Связанные документы (не дублировать, а ссылаться)

| Документ | Назначение |
|----------|------------|
| [phase1-rollout.md](./phase1-rollout.md) | Включение гибридного биллинга v1 в прод, env, проверки |
| [mariadb-migration-future.md](./mariadb-migration-future.md) | Перенос на MariaDB **позже** (сейчас не делаем) |

---

## 1. Цель проекта (one-liner)

Переработать **remna** в систему управления VPN с **гибридной тарификацией**: месячные пакеты (лимит ГБ + устройств) + **pay-as-you-go** сверх лимита; баланс в рублях с полом; учёт трафика и устройств по **вебхукам Remnawave**; детализация в боте, web-admin и тикетах; рефералы и промокоды на одном балансе; режим «оптимизированный маршрут» через squad + доплата за ГБ; перевыпуск подписки одной операцией API панели.

---

## 2. Зафиксированные решения (против «забыли обсудить»)

### 2.1. База данных

- **Сейчас:** только **PostgreSQL** (как в репозитории). Миграции Alembic, `asyncpg`.
- **Позже:** MariaDB — строго по [mariadb-migration-future.md](./mariadb-migration-future.md), отдельным эпиком **после** стабилизации логики на Postgres.

### 2.2. П.3 — Telegram: polling vs webhook (**решение автора плана**)

| Аспект | Решение |
|--------|---------|
| **По умолчанию** | Оставить **long polling** (`aiogram` + `start_polling`), как сейчас: меньше инфраструктурных зависимостей (публичный URL только для Telegram, сертификаты, секрет пути, антидубликаты). |
| **Вебхуки Remnawave** | Остаются **основным** источником событий трафика/устройств (как договорено). |
| **Telegram webhook** | **Не внедрять в первой волне.** Вынести в **фазу «опционально»**: включение через `TELEGRAM_WEBHOOK_*` + эндпоинт в `api`, когда будет явная потребность (масштаб, хостинг без исходящих). |

Итого: **ничего не ломаем в проде по Telegram**; фокус — биллинг и панель.

### 2.3. П.12 — Калькулятор (**нужен по ТЗ**)

- В боте (и при желании дубль в web-admin): экран **«Калькулятор»**.
- Пользователь вводит (или выбирает слайсерами) **число ГБ за период** и **число устройств·дней** (или сценарий «N устройств × 30 дней»).
- Вывод:
  - оценка **pay-as-you-go** (трафик + устройства + при включённом режиме — доплата за ГБ «оптимизированного маршрута»);
  - блок **«Сравнение с тарифом»**: пользователь выбирает **конкретный план из админки** (активные `Plan`) → показать **цена пакета** vs **оценка pay-as-you-go** за тот же период/объём (как в примере из ТЗ: 1 устр + 15 ГБ → 75 + 75 = 150 ₽).
- Использовать существующую логику оценки где возможно: например `shared/services/billing_calculator.py` (`estimate_pay_per_use_30d_rub`) — расширить под сценарии и надбавку маршрута.

### 2.4. Источник правды по трафику

- **Вебхуки панели** — основной канал.
- **Опционально позже:** минутный reconcile (опрос API) только как **страховка** при пропуске вебхуков — не в фазе 0, описать в фазе N после метрик.

### 2.5. Баланс и рефералы

- **Один** счёт: `users.balance` (рубли). Реферальные 10% с пополнений рефералов — **транзакции того же типа зачисления**, без отдельного «бонусного кошелька».

### 2.6. Первое пополнение

- Акции на **любое первое** успешное пополнение; конкретные пороги (например «+100% при первом пополнении от 10 ₽») и **+5 ГБ** — параметризовать в `Settings` / админке, не зашивать магические числа в хендлерах.

### 2.7. Устройства в один день

- Повторное добавление «того же» устройства в календарный день — **без второго списания за день**.
- Идентичность: **HWID + стабильные ключи из панели + имя устройства** (уточнить в реализации маппинг полей из payload вебхуков); идемпотентный ключ списания хранить в `BillingUsageEvent.event_id` / аналог.

### 2.8. «Оптимизированный маршрут»

- В нашей БД — **флаг пользователя** (или настройка подписки — уточнить в фазе модели).
- В Remnawave — **переключение squad** (UUID из конфига).
- Тарификация: **+2,5 ₽ за каждый оплачиваемый ГБ** в этом режиме (в дополнение к базовым 5 ₽/ГБ или к пакету — формализовать в матрице списаний в фазе 1).

### 2.9. Модель ГБ (п.5 из переписки)

Смысл (для реализации в коде, формулы — в отдельной подзадаче):

- Пока **не открыт** текущий оплаченный блок 1 ГБ: при **факте использования трафика** (первый байт / событие от панели — уточнить по типу вебхуков) **сразу списать 5 ₽** за блок **«до 1 ГБ»**.
- Следующие **5 ₽** — при потреблении **сверх** уже оплаченного целого гигабайта (ступени 1→2→3 ГБ относительно оплаченных блоков).
- Не смешивать с «пакетными» ГБ без явного правила: при активном пакете сначала **съедать пакетный лимит**, затем pay-as-you-go (согласовано в п.11 переписки).

---

## 3. Стек и сервисы (текущее remna)

| Компонент | Технология / расположение |
|-----------|---------------------------|
| Бот | Python 3.12, aiogram, **polling** (по умолчанию) |
| API | FastAPI, uvicorn, `api/main.py` |
| Web-admin | в `api/routers/web_admin.py` + шаблоны |
| БД | **PostgreSQL 16** (docker-compose), SQLAlchemy 2 async, Alembic |
| Кэш | Redis |
| Панель | Remnawave API + **вебхуки** (`REMNAWAVE_WEBHOOKS_*`) |
| Тикеты | отдельный бот `tickets/`, общая БД |
| Конфиг | `.env` + `shared/config.py` (`Settings`) |

---

## 4. Нефункциональные рекомендации

1. **Идемпотентность** всех списаний и вебхуков — не дублировать при ретраях панели (уже есть `RemnawaveWebhookEvent`, `BillingUsageEvent.event_id` — сохранять подход).
2. **Не раздувать scope:** одна фаза = один мержируемый результат + тесты.
3. **Отрицательный баланс и отключение:** единая политика (порог, уведомления, снятие squads) — сверяться с `billing_v2` / `negative_balance_notify_loop` и не плодить второй «движок».
4. **Логи и метрики:** при новых ветках биллинга — структурированный лог `user_id`, `event_id`, сумма.
5. **Документация API панели:** [Remnawave API](https://docs.rw/api) — для перевыпуска подписки искать **один** эндпоинт и зафиксировать его имя в этом файле после проверки на вашей версии панели.

---

## 5. Открытые вопросы к владельцу продукта (заполнять по мере ответов)

_Переносите ответы сюда, чтобы не потерять._

| # | Вопрос | Ответ |
|---|--------|--------|
| Q1 | Точные поля Remnawave webhook для «первого байта» vs «шаг ГБ» — какая версия панели стоит в проде? | |
| Q2 | Имя для режима «оптимизированный маршрут» в UX (нейтральное) — финальный текст кнопки? | |
| Q3 | Перевыпуск подписки: точное имя метода OpenAPI после проверки на панели? | |
| Q4 | Калькулятор: период сравнения по умолчанию — 30 календарных дней или до конца текущего месяца подписки? | |

---

## 6. Пошаговый план (фазы)

Отмечайте выполнение в PR или здесь при работе в ветке.

### Фаза 0 — Инвентаризация и спека (без большого кода)

- [ ] Снять текущую карту: `billing_v2/*`, `webhook_ingress_service`, `rating_service`, `Plan`, `Subscription`, тикеты (где показывать детализацию за день).
- [ ] Описать **state machine** списания ГБ (п.5) в виде таблицы: состояние пользователя → событие → списание → новое состояние.
- [ ] Сверить с [phase1-rollout.md](./phase1-rollout.md): что уже включено в проде, что выключено.
- [ ] Заполнить Q1–Q4 по возможности.

**Артефакт:** подпункты в §2.9 или отдельный `docs/billing-gb-state-machine.md` (если разрастётся).

### Фаза 1 — Модель ГБ «предоплата блока» + пакетный overage (п.5 + п.11)

- [ ] Формализовать счётчики: «оплаченные ГБ-блоки», «used в текущем периоде панели», взаимодействие с **пакетным** `monthly_gb_limit`.
- [ ] Адаптировать или заменить обработку вебхуков (`traffic.gb_step` и др.) под новую модель **без двойных списаний**.
- [ ] Юнит-тесты на краевые случаи: 0 трафика; 0.01 ГБ; ровно 1 ГБ; пакет исчерпан → payg.
- [ ] Обновить `.env.example` и комментарии в `Settings` для новых порогов/флагов.

### Фаза 2 — Устройства: полный день при добавлении + идемпотентность (п.7–8)

- [ ] Уточнить ключ идемпотентности (HWID + имя + …) в `charge_daily_device_once` / истории устройств.
- [ ] Согласовать с Remnawave payload поля «имя» и «ключи».
- [ ] Тесты: два attach одного логического устройства в один день.

### Фаза 3 — «Оптимизированный маршрут» (п.6)

- [ ] Миграция: флаг пользователя/подписки + опционально audit log.
- [ ] Конфиг: UUID squad «обычный» / «оптимизированный» в `Settings`.
- [ ] Вызовы Remnawave: переключение `active_internal_squads` (или эквивалент в вашей версии API).
- [ ] Надбавка **+2,5 ₽/ГБ** в `rating_service` / событиях с пометкой в `BillingUsageEvent.meta`.
- [ ] UX в боте: вкл/выкл + короткое объяснение.

### Фаза 4 — Баланс, пол, уведомления, отключение squads

- [ ] Сверка с `BILLING_BALANCE_FLOOR_RUB`, циклами уведомлений.
- [ ] Явное правило: при достижении пола → уведомление → **снятие squads** (и статус в RW) — один кодовый путь.
- [ ] Тесты на границу −50 ₽.

### Фаза 5 — Детализация: день / месяц (бот + web-admin + тикеты)

- [ ] Бот: меню «Моя подписка → Детализация» (день / месяц, формат как в ТЗ).
- [ ] Web-admin: профиль / биллинг — те же агрегаты.
- [ ] Тикеты: **только текущие сутки** (таймзона из `BILLING_CALENDAR_TIMEZONE` или явно UTC — зафиксировать в коде и здесь).
- [ ] Переиспользовать `billing_daily_summary` / `detail_service` где возможно.

### Фаза 6 — Первое пополнение и бонусы (п.10)

- [ ] Параметризовать: минимальная сумма для +100%, бонус ГБ после первого пополнения.
- [ ] Не ломать существующий `welcome_gb_bonus` — объединить логику или версионировать.

### Фаза 7 — Реферал 10% с пополнений на тот же баланс (п.9)

- [ ] Проверить текущий `REFERRAL_TOPUP_PERCENT` и зачисление на `user.balance`.
- [ ] Детализация транзакций в UI при необходимости.

### Фаза 8 — Калькулятор «план vs pay-as-you-go» (п.12)

- [ ] Хендлеры бота + клавиатура.
- [ ] Загрузка активных планов из БД (только не удалённые / активные для кнопок).
- [ ] Расширение `billing_calculator` или отдельный сервис «сравнение».
- [ ] По желанию: зеркало в web-admin.

### Фаза 9 — Перевыпуск подписки одной операцией RW (п.13)

- [ ] По [docs.rw/api](https://docs.rw/api) найти эндпоинт «reset / rotate subscription».
- [ ] Обернуть в `RemnaWaveClient` один метод `reset_user_subscription_credentials(...)` (имя уточнить по API).
- [ ] Бот: подтверждение «Вы уверены?» → вызов → отображение новой ссылки/инструкций.
- [ ] Записать в §5 Q3 финальный путь API.

### Фаза 10 — Конструктор тарифов в админке + кнопки в боте (п.12 / п.11)

- [ ] CRUD планов (название, цена, устройства, ГБ) — расширить существующий web-admin если уже частично есть.
- [ ] Синхронизация inline-кнопок «Название — Цена» при удалении плана.

### Фаза 11 — Опционально: Telegram webhook

- [ ] `TELEGRAM_WEBHOOK_URL`, секрет, маршрут в API, снятие polling при включении.
- [ ] Документация в README / phase1.

### Фаза 12 — MariaDB (отложено)

- [ ] Только по [mariadb-migration-future.md](./mariadb-migration-future.md) после стабилизации Postgres.

---

## 7. Критерии готовности «MVP переработки»

- Новая логика ГБ (п.5) + пакетный overage работают в тестах и на стейдже.
- Устройства и дневные списания соответствуют п.7–8.
- Детализация дня/месяца в боте; в тикетах — день.
- Калькулятор с блоком «сравнение с выбранным тарифом».
- Один баланс; реферал 10% на пополнения рефералов зачисляется на него.
- Режим «оптимизированный маршрут» включается и влияет на squad и на +2,5 ₽/ГБ.
- Перевыпуск подписки через **один** вызов API (после фиксации эндпоинта).

---

## 8. Чеклист для ассистента перед коммитом

- [ ] Изменения соответствуют **актуальной фазе** в §6.
- [ ] Обновлены тесты в `tests/`.
- [ ] При изменении поведения биллинга — **короткая запись** в этом файле (какая фаза/пункт закрыт) или в `docs/phase1-rollout.md` если про rollout.
- [ ] Не удалять этот файл из репозитория; конфликты мержа — разрешать с сохранением обоих наборов задач, затем дедупликация вручную.

---

## 9. История версий документа

| Версия | Дата | Изменения |
|--------|------|-----------|
| 1.0 | 2026-04-11 | Первый draft: требования, решения п.2–3–12, фазы, вопросы |
